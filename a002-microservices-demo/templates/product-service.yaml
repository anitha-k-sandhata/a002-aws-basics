apiVersion: v1
kind: Service
metadata:
  name: product-service

spec:
  selector:
    app: product-service

  ports:
    - protocol: TCP
      port: {{ .Values.product.servicePort }}
      targetPort: {{ .Values.product.servicePort }}

  type: ClusterIP
