apiVersion: v1
kind: Service
metadata:
  name: order-service

spec:
  selector:
    app: order-service

  ports:
    - protocol: TCP
      port: {{ .Values.order.servicePort }}
      targetPort: {{ .Values.order.servicePort }}

  type: NodePort
  