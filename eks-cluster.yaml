apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: a002-eks-demo
  region: ap-south-1

managedNodeGroups:
  - name: workers

    instanceType: t3.medium

    desiredCapacity: 1
    minSize: 1
    maxSize: 2

    volumeSize: 20

    ssh:
      allow: false
      